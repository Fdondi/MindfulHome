<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Launcher must see every installed app (Play-restricted; justified as default Home). -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Fallback foreground poll, most-used apps, and away detection. Not implied by
         Accessibility; prompting is skipped when app-switch detection is on. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Both required: generic FGS (API 28+) plus type-specific (API 34+).
         FOREGROUND_SERVICE_SPECIAL_USE does not replace FOREGROUND_SERVICE. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Start the process after reboot so USER_PRESENT can be registered in Application.onCreate. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Required for Gemini backend -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Nudge birds / chat overlay on top of other apps. Without this declaration,
         Settings greys out the "Display over other apps" toggle. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Discover LM Playground's exported AIDL even without QUERY_ALL_PACKAGES. -->
    <queries>
        <intent>
            <action android:name="com.druk.lmplayground.api.BIND_CHAT_SERVICE" />
        </intent>
    </queries>

    <application
        android:name=".MindfulHomeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindfulHome">

        <!-- Let AppCompat persist/apply per-app locales on API ≤ 32 (minSdk 29). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Credential Manager Play Services provider (pre-Android 14). Declared
             here so it cannot be dropped from the merged manifest. -->
        <service
            android:name="androidx.credentials.playservices.CredentialProviderMetadataHolder"
            android:enabled="true"
            android:exported="false">
            <meta-data
                android:name="androidx.credentials.CREDENTIAL_PROVIDER_KEY"
                android:value="androidx.credentials.playservices.CredentialProviderPlayServicesImpl" />
        </service>


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GoogleSignInActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity="${applicationId}.signin"
            android:alwaysRetainTaskState="true"
            android:autoRemoveFromRecents="true"
            android:theme="@style/Theme.MindfulHome.SignIn"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|locale|layoutDirection|density|uiMode|smallestScreenSize" />

        <activity
            android:name=".PinShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.MindfulHome">
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Optional: event-driven foreground-app detection (replaces the UsageStats poll).
             The user must enable it in system Accessibility settings; when off, the app
             falls back to polling. canRetrieveWindowContent=false in the config below. -->
        <service
            android:name=".service.ForegroundAppAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- USER_PRESENT and SCREEN_OFF are registered dynamically in MindfulHomeApp.onCreate()
             because Android 8+ doesn't deliver them to manifest receivers.
             BOOT_COMPLETED is exempted and works from the manifest. -->
        <receiver
            android:name=".receiver.ScreenUnlockReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Legacy browser "Add to Home screen" (e.g. Brave); delivered only to default launcher. -->
        <receiver
            android:name=".receiver.InstallShortcutReceiver"
            android:exported="true"
            android:permission="com.android.launcher.permission.INSTALL_SHORTCUT">
            <intent-filter>
                <action android:name="com.android.launcher.action.INSTALL_SHORTCUT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
